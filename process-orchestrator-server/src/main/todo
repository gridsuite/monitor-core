ajouter des TU
séparer en deux queues => une pour les step updates et l'autre pour les execution updates
nommage des queues
save process config

anything better than         context.getProcessContext().setLastExecutedStepId(context.getStepExecutionId());
 ?
//startedAt; // scheduled at
1 module for all

// process config : without executionID, without process_type ?

SA*** classes names
Add licences / headers
ajouter userId à l'exec pour savoir qui a lancé
//Id is generated in the worker for steps, is it ok?
order vs previousstepId?
jacoco aggregate
add swagger annotations
add tests for contexts
deployment => monitor profile